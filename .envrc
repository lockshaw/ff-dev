source_up

use flake "$PWD/.flakes#devShell.x86_64-linux"
PATH_add "$PWD/.scripts"

use vim .vimrc

export GH_DIR="$PWD"
CMAKE_LIBRARY_PATH="$CMAKE_LIBRARY_PATH:$(echo "$CMAKE_LIBRARY_PATH" | sed 's/:/\n/g' | grep 'cudatoolkit-11.7.0/lib$' | head -n 1)/stubs"
